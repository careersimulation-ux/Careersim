import { useAuth } from "@/_core/hooks/useAuth";
import { AppHeader } from "@/components/AppHeader";
import { useLanguage } from "@/i18n";
import { trpc } from "@/lib/trpc";
import { Check, GraduationCap, Loader2, Sparkles } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { toast } from "sonner";

const interestOptions = ["Data Analysis", "Business Intelligence", "Technology", "Business", "Finance", "Marketing", "Engineering", "Science"];
const countries = ["Saudi Arabia", "United Arab Emirates", "Qatar", "Kuwait", "Bahrain", "Oman"];

export default function Onboarding() {
  const { t, language, setLanguage } = useLanguage();
  const { user, loading } = useAuth({ redirectOnUnauthenticated: true });
  const [, setLocation] = useLocation();
  const [country, setCountry] = useState("Saudi Arabia");
  const [university, setUniversity] = useState("");
  const [major, setMajor] = useState("");
  const [graduationYear, setGraduationYear] = useState(new Date().getFullYear() + 1);
  const [interests, setInterests] = useState<string[]>(["Data Analysis"]);
  const mutation = trpc.profile.completeOnboarding.useMutation({
    onSuccess: () => { toast.success(language === "ar" ? "تم حفظ ملفك المهني" : "Your profile is ready"); setLocation("/catalog"); },
    onError: () => toast.error(t.common.error),
  });
  const toggleInterest = (interest: string) => setInterests(current => current.includes(interest) ? current.filter(item => item !== interest) : [...current, interest].slice(0, 3));
  const submit = (event: React.FormEvent) => { event.preventDefault(); mutation.mutate({ country, university, major, graduationYear, careerInterests: interests, preferredLanguage: language }); };
  if (loading || !user) return <div className="grid min-h-screen place-items-center"><Loader2 className="h-6 w-6 animate-spin text-teal-700" /></div>;
  return <div className="min-h-screen bg-[#f8fafc]"><AppHeader /><main className="container grid gap-10 py-10 lg:grid-cols-[0.85fr_1.15fr] lg:py-16"><section className="rounded-[2rem] bg-slate-950 p-7 text-white shadow-2xl shadow-slate-900/20 sm:p-10"><span className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-xs font-semibold text-teal-100"><Sparkles className="h-3.5 w-3.5" />{t.onboarding.eyebrow}</span><h1 className="mt-8 font-display text-4xl font-bold leading-[1.08] tracking-tight sm:text-5xl">{t.onboarding.title}</h1><p className="mt-5 max-w-md leading-7 text-slate-300">{t.onboarding.description}</p><div className="mt-14 space-y-5 border-t border-white/10 pt-7"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-teal-400/15 text-teal-200"><GraduationCap className="h-4 w-4" /></span><p className="text-sm text-slate-300">{language === "ar" ? "توصيات ومسارات مهارية ملائمة لاهتماماتك." : "Relevant simulations and skill pathways tailored to your interests."}</p></div><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-teal-400/15 text-teal-200"><Check className="h-4 w-4" /></span><p className="text-sm text-slate-300">{language === "ar" ? "ملف مهني قابل للمشاركة بعد إكمال محاكاتك." : "A shareable professional portfolio when you complete your simulations."}</p></div></div></section>
  <form onSubmit={submit} className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-[0_24px_60px_-42px_rgba(15,23,42,0.55)] sm:p-10"><div className="mb-8 flex items-center justify-between"><div><p className="text-sm font-semibold text-slate-950">{user.name ?? "CareerSim student"}</p><p className="mt-1 text-sm text-slate-500">{user.email}</p></div><button type="button" onClick={() => setLanguage(language === "en" ? "ar" : "en")} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-bold text-slate-600">{language === "en" ? "العربية" : "English"}</button></div><div className="grid gap-5 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold text-slate-700">{t.onboarding.country}<select value={country} onChange={e => setCountry(e.target.value)} className="h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm font-normal outline-none transition focus:border-teal-600 focus:ring-2 focus:ring-teal-100">{countries.map(item => <option key={item}>{item}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold text-slate-700">{t.onboarding.graduation}<input type="number" min="2020" max="2045" value={graduationYear} onChange={e => setGraduationYear(Number(e.target.value))} className="h-11 rounded-xl border border-slate-200 px-3 text-sm font-normal outline-none transition focus:border-teal-600 focus:ring-2 focus:ring-teal-100" /></label><label className="grid gap-2 text-sm font-semibold text-slate-700 sm:col-span-2">{t.onboarding.university}<input required value={university} onChange={e => setUniversity(e.target.value)} placeholder={language === "ar" ? "مثال: جامعة الملك سعود" : "e.g. King Saud University"} className="h-11 rounded-xl border border-slate-200 px-3 text-sm font-normal outline-none transition focus:border-teal-600 focus:ring-2 focus:ring-teal-100" /></label><label className="grid gap-2 text-sm font-semibold text-slate-700 sm:col-span-2">{t.onboarding.major}<input required value={major} onChange={e => setMajor(e.target.value)} placeholder={language === "ar" ? "مثال: علوم البيانات" : "e.g. Data Science"} className="h-11 rounded-xl border border-slate-200 px-3 text-sm font-normal outline-none transition focus:border-teal-600 focus:ring-2 focus:ring-teal-100" /></label></div><fieldset className="mt-7"><legend className="mb-3 text-sm font-semibold text-slate-700">{t.onboarding.interests}</legend><div className="flex flex-wrap gap-2">{interestOptions.map(interest => <button type="button" onClick={() => toggleInterest(interest)} key={interest} className={`rounded-full border px-3 py-2 text-xs font-semibold transition ${interests.includes(interest) ? "border-teal-700 bg-teal-700 text-white" : "border-slate-200 bg-white text-slate-600 hover:border-teal-300"}`}>{interest}</button>)}</div></fieldset><button type="submit" disabled={mutation.isPending || interests.length === 0} className="mt-9 flex w-full items-center justify-center gap-2 rounded-xl bg-teal-700 px-4 py-3.5 text-sm font-bold text-white shadow-lg shadow-teal-900/15 transition hover:bg-teal-800 active:scale-[0.98] disabled:opacity-60">{mutation.isPending && <Loader2 className="h-4 w-4 animate-spin" />}{mutation.isPending ? t.onboarding.saving : t.onboarding.continue}</button></form></main></div>;
}
