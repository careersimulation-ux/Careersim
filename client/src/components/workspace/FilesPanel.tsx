import { useLanguage } from "@/i18n";
import type { SalesRecord, SimulationConfig } from "@shared/simulation/types";
import { Download, FileBarChart, FileText, FolderOpen, X } from "lucide-react";
import { useEffect, useState } from "react";

export function FilesPanel({ files, records, onOpenData, selectedFileId, onClearSelectedFile }: { files: SimulationConfig["files"]; records: SalesRecord[]; onOpenData: () => void; selectedFileId?: string; onClearSelectedFile?: () => void }) {
  const { language, text } = useLanguage();
  const [openId, setOpenId] = useState<string | undefined>();
  useEffect(() => { if (selectedFileId) setOpenId(selectedFileId); }, [selectedFileId]);
  const selected = files.find(file => file.id === openId);
  const labels = language === "ar" ? { title: "ملفات الشركة", subtitle: "راجع المستندات لتحديد الأدلة المهمة لمهمتك.", open: "فتح", dataset: "فتح مساحة البيانات", download: "تنزيل", close: "إغلاق" } : { title: "Company files", subtitle: "Review documents and identify the evidence that matters to your assignment.", open: "Open", dataset: "Open data workspace", download: "Download", close: "Close" };
  const downloadCsv = () => {
    const header = Object.keys(records[0] ?? {}).join(",");
    const body = records.map(record => Object.values(record).map(value => JSON.stringify(value)).join(",")).join("\n");
    const url = URL.createObjectURL(new Blob([`${header}\n${body}`], { type: "text/csv" }));
    const anchor = document.createElement("a"); anchor.href = url; anchor.download = "CareerSim_Gulf_Retail_Sales_Data.csv"; anchor.click(); URL.revokeObjectURL(url);
  };
  const downloadDocument = (file: SimulationConfig["files"][number]) => {
    const extensionlessName = file.name.replace(/\.[^/.]+$/, "");
    const url = URL.createObjectURL(new Blob([text(file.content)], { type: "text/plain;charset=utf-8" }));
    const anchor = document.createElement("a"); anchor.href = url; anchor.download = `${extensionlessName}.txt`; anchor.click(); URL.revokeObjectURL(url);
  };
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-xl bg-teal-50 text-teal-700"><FolderOpen className="h-4 w-4" /></span><h1 className="font-display text-2xl font-bold tracking-tight text-slate-950">{labels.title}</h1></div><p className="mt-3 max-w-xl text-sm leading-6 text-slate-500">{labels.subtitle}</p></div></div><div className="mt-8 grid gap-3 md:grid-cols-2">{files.map(file => <article key={file.id} className="group rounded-2xl border border-slate-200 p-4 transition hover:border-teal-200 hover:shadow-lg hover:shadow-slate-900/5"><div className="flex gap-3"><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${file.kind === "dataset" ? "bg-teal-50 text-teal-700" : "bg-slate-100 text-slate-600"}`}>{file.kind === "dataset" ? <FileBarChart className="h-5 w-5" /> : <FileText className="h-5 w-5" />}</span><div className="min-w-0"><p className="truncate text-sm font-bold text-slate-900">{file.name}</p><p className="mt-1 text-xs leading-5 text-slate-500">{text(file.description)}</p></div></div><div className="mt-4 flex gap-2">{file.kind === "dataset" ? <><button type="button" onClick={onOpenData} className="flex-1 rounded-lg bg-teal-700 px-3 py-2 text-xs font-bold text-white transition hover:bg-teal-800">{labels.dataset}</button><button type="button" onClick={downloadCsv} className="grid h-8 w-8 place-items-center rounded-lg border border-slate-200 text-slate-600 transition hover:bg-slate-50" aria-label={labels.download}><Download className="h-3.5 w-3.5" /></button></> : <><button type="button" onClick={() => setOpenId(file.id)} className="flex-1 rounded-lg border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700 transition hover:border-teal-200 hover:bg-teal-50">{labels.open}</button><button type="button" onClick={() => downloadDocument(file)} className="grid h-8 w-8 place-items-center rounded-lg border border-slate-200 text-slate-600 transition hover:bg-slate-50" aria-label={`${labels.download} ${file.name}`}><Download className="h-3.5 w-3.5" /></button></>}</div></article>)}</div>{selected && <div className="mt-6 rounded-2xl border border-teal-100 bg-teal-50/50 p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700">{selected.name}</p><h2 className="mt-2 font-display text-lg font-bold text-slate-950">{text(selected.label)}</h2></div><button type="button" onClick={() => { setOpenId(undefined); onClearSelectedFile?.(); }} className="grid h-8 w-8 place-items-center rounded-full bg-white text-slate-500"><X className="h-4 w-4" /></button></div><p className="mt-4 whitespace-pre-line text-sm leading-7 text-slate-700">{text(selected.content)}</p><div className="mt-5 flex gap-2"><button type="button" onClick={() => downloadDocument(selected)} className="inline-flex items-center gap-2 rounded-lg bg-teal-700 px-3 py-2 text-xs font-bold text-white"><Download className="h-3.5 w-3.5" />{labels.download}</button><button type="button" onClick={() => { setOpenId(undefined); onClearSelectedFile?.(); }} className="rounded-lg bg-white px-3 py-2 text-xs font-bold text-slate-700 shadow-sm">{labels.close}</button></div></div>}</section>;
}
